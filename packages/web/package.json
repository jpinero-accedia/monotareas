{
	"name": "@monotareas/web",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"clean": "rm -rf dist",
		"type": "tsc -p tsconfig.json",
		"esbuild": "tsx build.ts",
		"build": "pnpm type && pnpm esbuild",
		"cbuild": "pnpm clean && pnpm build",
		"clean-and-build": "pnpm cbuild",
		"cb": "pnpm cbuild",
		"dev": "tsx build.ts --watch"
	},
	"dependencies": {
		"@ungap/custom-elements": "^1.3.0"
	},
	"devDependencies": {
		"esbuild": "^0.28.1",
		"tsx": "^4.22.4",
		"typescript": "^6.0.3"
	}
}
